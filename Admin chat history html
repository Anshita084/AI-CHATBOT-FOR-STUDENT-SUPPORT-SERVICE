{% extends "base.html" %}
{% block title %}Chat History{% endblock %}

{% block content %}
<div class="page-container">
    <div class="admin-header">
        <h1>Chat History</h1>
        <a href="{{ url_for('admin_dashboard') }}" class="btn-view">← Back to Dashboard</a>
    </div>

    <table class="history-table">
        <thead>
            <tr>
                <th>#</th>
                <th>Student</th>
                <th>Query</th>
                <th>Response</th>
                <th>Date / Time</th>
            </tr>
        </thead>
        <tbody>
            {% for row in history %}
            <tr>
                <td>{{ row.Chat_ID }}</td>
                <td>{{ row.Student_Name or "Guest" }}</td>
                <td>{{ row.Query }}</td>
                <td>{{ row.Response }}</td>
                <td>{{ row.Date_Time }}</td>
            </tr>
            {% else %}
            <tr><td colspan="5" class="empty">No chat history yet.</td></tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
